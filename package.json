{
  "name": "router-tracker",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "nodemon --config nodemon.json src/index.js",
    "test-dev": "node src/index.js",
    "build": "rm -rf dist && tsc"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "crypto-js": "^4.1.1",
    "dotenv": "^16.0.0",
    "node-fetch": "^3.2.0",
    "node-schedule": "^2.1.0"
  }
}
